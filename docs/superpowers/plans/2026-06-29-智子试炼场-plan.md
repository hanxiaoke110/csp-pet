# 智子试炼场 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将现有「潜龙闭关」地牢模式的答题扣血战斗，替换为「宠物回合制对战 + 编程题驱动技能释放」的新战斗系统。

**Architecture:** 保留现有 8 副本 40 关、CSP 真题题库和 D1 后端结构；在前端新增宠物战斗属性、技能系统、元素克制、手动升级；重写 `BattleScreen` 战斗表现层；后端扩展排行榜维度。

**Tech Stack:** Tauri v2 + React 19 + TypeScript + Zustand + Vite；Cloudflare Worker + D1 后端。

---

## File Structure

### 新增文件

| 文件 | 职责 |
|---|---|
| `src-dungeon/utils/combatLogic.ts` | 战斗数值计算（伤害、元素克制、评级） |
| `src-dungeon/utils/combatLogic.test.ts` | 战斗逻辑单元测试 |
| `src-dungeon/data/skills.ts` | 4 技能定义（知识点标签、倍率、冷却） |
| `src-dungeon/components/screens/SkillTooltip.tsx` | 技能 tooltip 组件 |
| `src-dungeon/components/screens/TutorialOverlay.tsx` | 首次战斗新手引导 |
| `src-dungeon/components/screens/ElementGuide.tsx` | 元素克制手册弹窗 |

### 修改文件

| 文件 | 职责 |
|---|---|
| `src/types/pet.ts` | 宠物类型新增战斗属性 |
| `src/stores/petStore.ts` | 手动升级、升级回血、属性计算 |
| `src-dungeon/types/dungeon.ts` | 战斗状态、敌方宠物、技能类型 |
| `src-dungeon/utils/gameLogic.ts` | 奖励、评级公式 |
| `src-dungeon/utils/questionLoader.ts` | 按技能标签加载题目 |
| `src-dungeon/stores/dungeonStore.ts` | 战斗状态管理、周挑战次数 |
| `src-dungeon/components/screens/BattleScreen.tsx` | 新战斗 UI |
| `src-dungeon/components/screens/RewardScreen.tsx` | 结算展示评级维度 |
| `src-dungeon/components/screens/LeaderboardScreen.tsx` | 班级排行榜扩展 |
| `src-dungeon/data/dungeons.json` | 每关添加敌方宠物配置 |
| `cf-workers/api.js` | 排行榜新维度、战斗结果字段 |

---

## Stage 1: 核心战斗系统

### Task 1: 扩展宠物类型定义

**Files:**
- Modify: `src/types/pet.ts`
- Test: `src/types/pet.test.ts`（如不存在则创建）

**Context:** 现有 `OwnedPet` 已有 `level`、`exp`、`expToNext` 等字段，需要新增战斗属性。

- [ ] **Step 1: 在 `src/types/pet.ts` 顶部新增 `BattleStats` 接口**

```typescript
export interface BattleStats {
  maxHp: number;
  currentHp: number;
  attack: number;
  defense: number;
  speed: number;
}
```

- [ ] **Step 2: 在 `OwnedPet` 接口中新增字段**

```typescript
export interface OwnedPet {
  // ... 现有字段保持不变
  level: number;
  exp: number;
  expToNext: number;
  // 新增
  battle?: BattleStats;
  expPool?: number; // 战斗获得的经验先存入池，手动分配
}
```

- [ ] **Step 3: 添加基础属性配置表**

在 `src/types/pet.ts` 末尾新增：

```typescript
export const PET_BASE_STATS: Record<string, Omit<BattleStats, 'currentHp'>> = {
  capi:   { maxHp: 80,  attack: 12, defense: 8,  speed: 8 },
  boba:   { maxHp: 70,  attack: 10, defense: 7,  speed: 12 },
  'bubu-2': { maxHp: 75, attack: 14, defense: 6,  speed: 10 },
  miga:   { maxHp: 65,  attack: 11, defense: 6,  speed: 14 },
  default:{ maxHp: 70,  attack: 11, defense: 7,  speed: 10 },
};

export const TIER_MULTIPLIERS: Record<PetTier, number> = {
  common: 1.0,
  rare: 1.3,
  legendary: 1.6,
};
```

- [ ] **Step 4: 提交**

```bash
git add src/types/pet.ts
git commit -m "feat(智子试炼场): 为宠物类型新增战斗属性与基础数值配置"
```

---

### Task 2: 创建战斗数值逻辑模块

**Files:**
- Create: `src-dungeon/utils/combatLogic.ts`
- Create: `src-dungeon/utils/combatLogic.test.ts`

**Context:** 所有战斗数值计算集中在这个模块，便于测试和复用。

- [ ] **Step 1: 创建 `src-dungeon/utils/combatLogic.ts`**

```typescript
import { PetElement } from '../../src/types/pet';

export interface CombatPet {
  maxHp: number;
  currentHp: number;
  attack: number;
  defense: number;
  speed: number;
  element: PetElement;
  level: number;
}

export const ELEMENT_ADVANTAGE: Record<PetElement, Partial<Record<PetElement, number>>> = {
  fire:  { wind: 1.5, water: 0.7 },
  wind:  { earth: 1.5, fire: 0.7 },
  earth: { water: 1.5, wind: 0.7 },
  water: { fire: 1.5, earth: 0.7 },
  light: {},
};

export function getElementMultiplier(attacker: PetElement, defender: PetElement): number {
  return ELEMENT_ADVANTAGE[attacker]?.[defender] ?? 1.0;
}

export function calculateDamage(
  attacker: CombatPet,
  defender: CombatPet,
  skillMultiplier: number,
  answerQuality: number
): number {
  const raw = attacker.attack * skillMultiplier - defender.defense;
  const elementMultiplier = getElementMultiplier(attacker.element, defender.element);
  const damage = Math.max(1, Math.floor(raw * elementMultiplier * answerQuality));
  return damage;
}

export function calculateStats(
  base: { maxHp: number; attack: number; defense: number; speed: number },
  tierMultiplier: number,
  level: number
): Omit<CombatPet, 'currentHp' | 'element'> {
  return {
    maxHp: Math.floor(base.maxHp * tierMultiplier * Math.pow(1.1, level - 1)),
    attack: Math.floor(base.attack * tierMultiplier * Math.pow(1.1, level - 1)),
    defense: Math.floor(base.defense * tierMultiplier * Math.pow(1.1, level - 1)),
    speed: Math.floor(base.speed * tierMultiplier * Math.pow(1.05, level - 1)),
  };
}

export function determineFirstAttacker(player: CombatPet, enemy: CombatPet): 'player' | 'enemy' {
  return player.speed >= enemy.speed ? 'player' : 'enemy';
}
```

- [ ] **Step 2: 创建 `src-dungeon/utils/combatLogic.test.ts`**

```typescript
import { describe, it, expect } from 'vitest';
import { calculateDamage, getElementMultiplier, determineFirstAttacker } from './combatLogic';
import { PetElement } from '../../src/types/pet';

const makePet = (element: PetElement, attack: number, defense: number, speed: number) => ({
  maxHp: 100, currentHp: 100, attack, defense, speed, element, level: 1,
});

describe('元素克制', () => {
  it('火克风', () => {
    expect(getElementMultiplier('fire', 'wind')).toBe(1.5);
  });
  it('火被水克', () => {
    expect(getElementMultiplier('fire', 'water')).toBe(0.7);
  });
  it('光无克制', () => {
    expect(getElementMultiplier('light', 'fire')).toBe(1.0);
  });
});

describe('伤害计算', () => {
  it('基础伤害', () => {
    const attacker = makePet('fire', 20, 5, 10);
    const defender = makePet('wind', 10, 5, 8);
    const dmg = calculateDamage(attacker, defender, 1.0, 1.0);
    expect(dmg).toBeGreaterThan(0);
  });
});

describe('先手判定', () => {
  it('速度快先攻', () => {
    const player = makePet('fire', 10, 5, 12);
    const enemy = makePet('water', 10, 5, 8);
    expect(determineFirstAttacker(player, enemy)).toBe('player');
  });
});
```

- [ ] **Step 3: 运行测试**

```bash
cd /Users/hanliuliu/Desktop/学生成长计划/csp学习助手（师+生）/csp-desktop-pet
npm test -- src-dungeon/utils/combatLogic.test.ts
```

Expected: all tests pass.

- [ ] **Step 4: 提交**

```bash
git add src-dungeon/utils/combatLogic.ts src-dungeon/utils/combatLogic.test.ts
git commit -m "feat(智子试炼场): 添加战斗数值逻辑与单元测试"
```

---

### Task 3: 创建技能定义

**Files:**
- Create: `src-dungeon/data/skills.ts`

**Context:** 4 个技能对应 4 类 CSP 知识点，配置冷却和倍率。

- [ ] **Step 1: 创建 `src-dungeon/data/skills.ts`**

```typescript
export type KnowledgeTag = 'grammar' | 'control-flow' | 'data-structure' | 'algorithm';

export interface SkillDefinition {
  id: string;
  name: string;
  knowledgeTag: KnowledgeTag;
  knowledgeLabel: string; // 给孩子看的中文名
  multiplier: number;
  cooldown: number; // 回合数，0 表示无冷却
  maxUsesPerBattle: number | null; // null 表示无限制
  description: string;
}

export const SKILLS: SkillDefinition[] = [
  {
    id: 'skill-1',
    name: '语法射线',
    knowledgeTag: 'grammar',
    knowledgeLabel: '语法基础',
    multiplier: 1.0,
    cooldown: 0,
    maxUsesPerBattle: null,
    description: '语法基础题驱动的普通攻击',
  },
  {
    id: 'skill-2',
    name: '循环火球',
    knowledgeTag: 'control-flow',
    knowledgeLabel: '流程控制',
    multiplier: 1.2,
    cooldown: 1,
    maxUsesPerBattle: null,
    description: '流程控制题驱动的元素攻击',
  },
  {
    id: 'skill-3',
    name: '数组护盾',
    knowledgeTag: 'data-structure',
    knowledgeLabel: '数据结构',
    multiplier: 1.4,
    cooldown: 2,
    maxUsesPerBattle: null,
    description: '数据结构题驱动的攻击或防御技能',
  },
  {
    id: 'skill-4',
    name: '递归爆发',
    knowledgeTag: 'algorithm',
    knowledgeLabel: '算法思维',
    multiplier: 1.8,
    cooldown: 3,
    maxUsesPerBattle: 2,
    description: '算法题驱动的大招，每关限用 2 次',
  },
];

export function getSkillById(id: string): SkillDefinition | undefined {
  return SKILLS.find(s => s.id === id);
}
```

- [ ] **Step 2: 提交**

```bash
git add src-dungeon/data/skills.ts
git commit -m "feat(智子试炼场): 定义 4 个技能与知识点标签"
```

---

### Task 4: 扩展地牢类型定义

**Files:**
- Modify: `src-dungeon/types/dungeon.ts`

**Context:** 新增战斗状态、敌方宠物配置、技能使用记录等类型。

- [ ] **Step 1: 在 `src-dungeon/types/dungeon.ts` 中新增类型**

```typescript
import { PetElement, PetTier } from '../../src/types/pet';
import { KnowledgeTag } from '../data/skills';

// 敌方宠物配置
export interface EnemyPetConfig {
  speciesId: string;
  displayName: string;
  level: number;
  tier: PetTier;
  element: PetElement;
  maxHpBoost?: number; // 额外血量倍率，Boss 可用
}

// 战斗中的技能使用记录
export interface SkillUsage {
  skillId: string;
  usedCount: number;
  cooldownRemaining: number;
}

// 扩展 BattleState
export interface BattleState {
  dungeonId: string;
  stageId: string;
  questions: Question[];
  currentQuestionIndex: number;
  hp: number;
  maxHp: number;
  correctCount: number;
  wrongCount: number;
  comboCount: number;
  startTime: number;
  isBoss: boolean;
  isFinished: boolean;
  isWon: boolean;
  expEarned: number;
  goldEarned: number;
  rating: string;
  // 新增
  enemyHp: number;
  enemyMaxHp: number;
  currentTurn: 'player' | 'enemy';
  roundCount: number;
  skillUsages: SkillUsage[];
  usedSkillIds: string[];
}
```

- [ ] **Step 2: 在 `DungeonStage` 中添加敌方宠物配置**

```typescript
export interface DungeonStage {
  id: string;
  name: string;
  description: string;
  questionIds: string[];
  requiredCorrect: number; // 旧字段保留兼容
  hp: number; // 旧字段，新战斗中可忽略或作为玩家容错参考
  enemyPet?: EnemyPetConfig; // 新增
}
```

- [ ] **Step 3: 提交**

```bash
git add src-dungeon/types/dungeon.ts
git commit -m "feat(智子试炼场): 扩展地牢类型以支持宠物战斗"
```

---

### Task 5: 更新题目加载器（按技能标签选题）

**Files:**
- Modify: `src-dungeon/utils/questionLoader.ts`

**Context:** 玩家选择技能后，需要从题库中抽取对应知识点标签的题目。

- [ ] **Step 1: 在 `questionLoader.ts` 中添加按标签选题函数**

```typescript
import { KnowledgeTag } from '../data/skills';

export function pickQuestionsByTag(
  allQuestions: Question[],
  tag: KnowledgeTag,
  count: number
): Question[] {
  const tagMap: Record<KnowledgeTag, string[]> = {
    'grammar': ['语法', '变量', '数据类型', '运算符'],
    'control-flow': ['分支', '循环', 'if', 'for', 'while'],
    'data-structure': ['数组', '字符串', '栈', '队列', '树', '结构'],
    'algorithm': ['枚举', '递归', '排序', '贪心', '搜索', '算法'],
  };

  const keywords = tagMap[tag];
  const matched = allQuestions.filter(q =>
    keywords.some(kw =>
      (q.knowledgePoint?.includes(kw)) ||
      (q.question?.includes(kw))
    )
  );

  // 随机抽取 count 道，不足则全取
  const shuffled = [...matched].sort(() => Math.random() - 0.5);
  return shuffled.slice(0, count);
}
```

- [ ] **Step 2: 提交**

```bash
git add src-dungeon/utils/questionLoader.ts
git commit -m "feat(智子试炼场): 按技能知识点标签抽取题目"
```

---

### Task 6: 更新宠物 Store（手动升级与战斗属性初始化）

**Files:**
- Modify: `src/stores/petStore.ts`

**Context:** 宠物获得经验后先存入 `expPool`，玩家手动点击升级时消耗经验并回满 HP。

- [ ] **Step 1: 在 `petStore` 的 state 中添加 `expPool`**

```typescript
interface PetState {
  // ... 现有字段
  activePetId: string | null;
  // 新增
  expPool: number;
}
```

- [ ] **Step 2: 添加经验获取和手动升级方法**

```typescript
addExpToPool(amount: number) {
  set(state => ({ expPool: state.expPool + amount }));
},

canLevelUp(petId: string): boolean {
  const pet = get().pets.find(p => p.petId === petId);
  if (!pet) return false;
  return get().expPool >= pet.expToNext;
},

levelUp(petId: string) {
  const pet = get().pets.find(p => p.petId === petId);
  if (!pet || !get().canLevelUp(petId)) return;

  const needed = pet.expToNext;
  const newLevel = pet.level + 1;
  const newExpToNext = Math.floor(needed * 1.3);

  // 计算新属性
  const speciesId = pet.speciesId;
  const base = PET_BASE_STATS[speciesId] || PET_BASE_STATS.default;
  const tier = getPetTier(speciesId);
  const stats = calculateStats(base, TIER_MULTIPLIERS[tier], newLevel);

  set(state => ({
    expPool: state.expPool - needed,
    pets: state.pets.map(p =>
      p.petId === petId
        ? {
            ...p,
            level: newLevel,
            exp: 0,
            expToNext: newExpToNext,
            battle: {
              ...stats,
              currentHp: stats.maxHp,
            },
          }
        : p
    ),
  }));
},
```

- [ ] **Step 3: 在初始化/加载宠物时补充 battle 字段**

```typescript
ensureBattleStats(pet: OwnedPet): OwnedPet {
  if (pet.battle) return pet;
  const base = PET_BASE_STATS[pet.speciesId] || PET_BASE_STATS.default;
  const tier = getPetTier(pet.speciesId);
  const stats = calculateStats(base, TIER_MULTIPLIERS[tier], pet.level);
  return {
    ...pet,
    battle: { ...stats, currentHp: stats.maxHp },
  };
}
```

- [ ] **Step 4: 提交**

```bash
git add src/stores/petStore.ts
git commit -m "feat(智子试炼场): 宠物手动升级与战斗属性初始化"
```

---

### Task 7: 重写 BattleScreen

**Files:**
- Modify: `src-dungeon/components/screens/BattleScreen.tsx`
- Create: `src-dungeon/components/screens/SkillTooltip.tsx`

**Context:** 这是最大的 UI 改动，需要展示双方宠物、HP 条、技能按钮、题目面板。

- [ ] **Step 1: 创建 `SkillTooltip.tsx`**

```typescript
import { SkillDefinition } from '../../data/skills';

interface Props {
  skill: SkillDefinition;
  cooldownRemaining: number;
  usesLeft: number | null;
}

export function SkillTooltip({ skill, cooldownRemaining, usesLeft }: Props) {
  return (
    <div className="skill-tooltip">
      <strong>{skill.name}</strong>
      <p>{skill.description}</p>
      <p>知识点：{skill.knowledgeLabel}</p>
      <p>伤害倍率：{skill.multiplier}×</p>
      {skill.cooldown > 0 && <p>冷却：{skill.cooldown} 回合</p>}
      {usesLeft !== null && <p>本关剩余：{usesLeft} 次</p>}
      {cooldownRemaining > 0 && <p className="cooldown">冷却中：还剩 {cooldownRemaining} 回合</p>}
    </div>
  );
}
```

- [ ] **Step 2: 重写 `BattleScreen.tsx` 核心结构**

由于原文件较长，这里给出核心 JSX 结构：

```tsx
<div className="battle-screen">
  {/* 顶部回合信息 */}
  <div className="battle-header">
    <span>回合 {battle.roundCount}</span>
    <span>{battle.currentTurn === 'player' ? '你的回合' : '敌方回合'}</span>
  </div>

  {/* 对战双方 */}
  <div className="battle-arena">
    <div className="pet-side player">
      <PetSprite pet={playerPet} animation="idle" />
      <HpBar current={battle.hp} max={battle.maxHp} />
      <span>{playerPet.petName}</span>
    </div>

    <div className="pet-side enemy">
      <PetSprite pet={enemyPet} animation="idle" />
      <HpBar current={battle.enemyHp} max={battle.enemyMaxHp} />
      <span>{enemyPet.displayName}</span>
    </div>
  </div>

  {/* 技能按钮（仅玩家回合） */}
  {battle.currentTurn === 'player' && (
    <div className="skill-bar">
      {SKILLS.map(skill => {
        const usage = battle.skillUsages.find(u => u.skillId === skill.id);
        const onCooldown = usage?.cooldownRemaining > 0;
        const noUsesLeft = skill.maxUsesPerBattle !== null && usage?.usedCount >= skill.maxUsesPerBattle;
        const disabled = onCooldown || noUsesLeft;

        return (
          <button
            key={skill.id}
            className="skill-btn"
            disabled={disabled}
            onClick={() => onSelectSkill(skill.id)}
          >
            <SkillTooltip skill={skill} usage={usage} />
            {skill.name}
          </button>
        );
      })}
    </div>
  )}

  {/* 题目面板 */}
  {showQuestion && (
    <QuestionPanel
      question={currentQuestion}
      onAnswer={onAnswer}
    />
  )}
</div>
```

- [ ] **Step 3: 实现战斗状态流转逻辑**

伪代码：

```typescript
function onSelectSkill(skillId: string) {
  const skill = getSkillById(skillId);
  const question = pickQuestionsByTag(allQuestions, skill.knowledgeTag, 1)[0];
  setPendingSkill(skillId);
  setCurrentQuestion(question);
  setShowQuestion(true);
}

function onAnswer(selectedIndex: number) {
  const isCorrect = selectedIndex === currentQuestion.correctIndex;
  const skill = getSkillById(pendingSkill);
  const quality = isCorrect ? 1.0 : 0.6;

  // 计算伤害
  const damage = calculateDamage(playerBattleStats, enemyBattleStats, skill.multiplier, quality);

  // 更新敌方 HP
  updateBattleState(draft => {
    draft.enemyHp = Math.max(0, draft.enemyHp - damage);
    draft.correctCount += isCorrect ? 1 : 0;
    draft.wrongCount += isCorrect ? 0 : 1;
    draft.usedSkillIds = [...draft.usedSkillIds, skill.id];
    // 更新技能冷却和使用次数
    updateSkillUsage(draft, skill.id);
  });

  setShowQuestion(false);

  if (battle.enemyHp <= 0) {
    endBattle(true);
  } else {
    // 敌方回合
    enemyTurn();
  }
}

function enemyTurn() {
  // AI 随机选技能，可后续优化
  const damage = calculateDamage(enemyBattleStats, playerBattleStats, 1.0, 1.0);
  updateBattleState(draft => {
    draft.hp = Math.max(0, draft.hp - damage);
    draft.currentTurn = 'player';
  });

  if (battle.hp <= 0) {
    endBattle(false);
  }
}
```

- [ ] **Step 4: 提交**

```bash
git add src-dungeon/components/screens/BattleScreen.tsx src-dungeon/components/screens/SkillTooltip.tsx
git commit -m "feat(智子试炼场): 重写战斗界面与回合逻辑"
```

---

## Stage 2: 关卡配置与经济系统

### Task 8: 为每关配置敌方宠物

**Files:**
- Modify: `src-dungeon/data/dungeons.json`

**Context:** 给每个 stage 添加 `enemyPet` 配置。

- [ ] **Step 1: 为每个 stage 添加敌方宠物**

示例（天机阁第 1 关）：

```json
{
  "id": "dungeon-01-stage-01",
  "name": "初识天机",
  "description": "计算机发展史与基础概念",
  "questionIds": [],
  "requiredCorrect": 3,
  "hp": 3,
  "enemyPet": {
    "speciesId": "glitch-bot",
    "displayName": "蓝屏幽魂·小卒",
    "level": 1,
    "tier": "common",
    "element": "water"
  }
}
```

- [ ] **Step 2: 为 Boss 关配置更强敌方宠物**

```json
{
  "id": "dungeon-01-stage-05",
  "name": "天机试炼",
  "description": "计算机基础综合挑战",
  "questionIds": [],
  "requiredCorrect": 4,
  "hp": 2,
  "enemyPet": {
    "speciesId": "glitch-bot",
    "displayName": "蓝屏幽魂",
    "level": 3,
    "tier": "rare",
    "element": "water",
    "maxHpBoost": 1.5
  }
}
```

- [ ] **Step 3: 提交**

```bash
git add src-dungeon/data/dungeons.json
git commit -m "feat(智子试炼场): 为 8 副本 40 关配置敌方宠物"
```

---

### Task 9: 实现随机金币奖励

**Files:**
- Modify: `src-dungeon/utils/gameLogic.ts`

**Context:** 所有奖励改为随机金币范围。

- [ ] **Step 1: 添加随机奖励函数**

```typescript
export function randomGold(min: number, max: number): number {
  return Math.floor(Math.random() * (max - min + 1)) + min;
}

export function calculateBattleRewards(
  isWin: boolean,
  isFirstClear: boolean,
  isBoss: boolean,
  rating: string
): { gold: number; breakdown: string[] } {
  if (!isWin) return { gold: 0, breakdown: ['失败，无奖励'] };

  const breakdown: string[] = [];
  let gold = randomGold(10, 20);
  breakdown.push(`胜利奖励 ${gold} 金币`);

  if (isFirstClear) {
    const bonus = gold * 2;
    gold += bonus;
    breakdown.push(`首次通关 ×2：+${bonus} 金币`);
  }

  if (isBoss) {
    const bonus = randomGold(15, 30);
    gold += bonus;
    breakdown.push(`Boss 奖励：+${bonus} 金币`);
  }

  if (rating === 'S') {
    const bonus = randomGold(10, 15);
    gold += bonus;
    breakdown.push(`S 评级奖励：+${bonus} 金币`);
  } else if (rating === 'SS') {
    const bonus = randomGold(15, 20);
    gold += bonus;
    breakdown.push(`SS 评级奖励：+${bonus} 金币`);
  }

  return { gold, breakdown };
}
```

- [ ] **Step 2: 提交**

```bash
git add src-dungeon/utils/gameLogic.ts
git commit -m "feat(智子试炼场): 随机金币奖励计算"
```

---

### Task 10: 实现 S/SS 评级

**Files:**
- Modify: `src-dungeon/utils/gameLogic.ts`

**Context:** 根据正确率、HP、技能多样性、回合数评级。

- [ ] **Step 1: 添加评级函数**

```typescript
export function calculateBattleRating(
  correctCount: number,
  totalQuestions: number,
  remainingHpRatio: number,
  usedSkillIds: string[],
  roundCount: number,
  expectedRounds: number
): ClearRating {
  const accuracy = totalQuestions > 0 ? correctCount / totalQuestions : 0;
  const uniqueSkills = new Set(usedSkillIds).size;

  if (accuracy === 1 && remainingHpRatio >= 0.7 && uniqueSkills >= 4 && roundCount <= expectedRounds) {
    return 'SS';
  }
  if (accuracy >= 0.8 && remainingHpRatio >= 0.5 && uniqueSkills >= 3) {
    return 'S';
  }
  if (accuracy >= 0.7 && remainingHpRatio >= 0.3) return 'A';
  if (accuracy >= 0.6) return 'B';
  return 'C';
}
```

- [ ] **Step 2: 提交**

```bash
git add src-dungeon/utils/gameLogic.ts
git commit -m "feat(智子试炼场): 战斗评级计算"
```

---

### Task 11: 实现每周 5 次挑战限制

**Files:**
- Modify: `src-dungeon/stores/dungeonStore.ts`
- Modify: `cf-workers/api.js`

**Context:** 每周可挑战 5 次，次数用完仍可练习但无金币。

- [ ] **Step 1: 在 `dungeonStore` 添加周挑战状态**

```typescript
interface DungeonStore {
  // ... 现有字段
  weeklyChallenges: {
    used: number;
    limit: number;
    resetAt: string; // ISO 周一开始
  };
}
```

- [ ] **Step 2: 添加消耗次数和检查方法**

```typescript
function getWeekStart(): string {
  const now = new Date();
  const day = now.getDay();
  const diff = now.getDate() - day + (day === 0 ? -6 : 1);
  const monday = new Date(now.setDate(diff));
  monday.setHours(0, 0, 0, 0);
  return monday.toISOString();
}

canEarnRewards(): boolean {
  const state = get();
  return state.weeklyChallenges.used < state.weeklyChallenges.limit;
},

useChallenge() {
  const state = get();
  const currentWeekStart = getWeekStart();
  if (state.weeklyChallenges.resetAt !== currentWeekStart) {
    set({
      weeklyChallenges: { used: 0, limit: 5, resetAt: currentWeekStart },
    });
  }
  set(state => ({
    weeklyChallenges: {
      ...state.weeklyChallenges,
      used: state.weeklyChallenges.used + 1,
    },
  }));
},
```

- [ ] **Step 3: 后端在 `dungeon_attempts` 记录中增加 `earned_reward` 字段**

在 `cf-workers/api.js` 中 `dungeon_attempts` 表结构新增：

```sql
earned_reward INTEGER DEFAULT 0
```

- [ ] **Step 4: 提交**

```bash
git add src-dungeon/stores/dungeonStore.ts cf-workers/api.js
git commit -m "feat(智子试炼场): 每周 5 次挑战次数限制"
```

---

## Stage 3: 排行榜、背景、说明系统

### Task 12: 扩展班级排行榜

**Files:**
- Modify: `cf-workers/api.js`
- Modify: `src-dungeon/components/screens/LeaderboardScreen.tsx`

**Context:** 为智子试炼场新增排行榜维度。

- [ ] **Step 1: 在 `cf-workers/api.js` 扩展排行榜类型**

在 `/api/dungeon/leaderboard` 处理中新增：

```javascript
const VALID_TYPES = ['power', 'streak', 'conquest', 'badge', 'wins', 'ss_count', 'progress', 'warrior'];
```

并为新类型写 SQL：

```javascript
case 'wins':
  sql = `SELECT display_name, school, rank_tier, COUNT(*) as value
         FROM dungeon_attempts
         WHERE class_code = ? AND is_win = 1 AND created_at >= ?
         GROUP BY device_hash
         ORDER BY value DESC
         LIMIT 50`;
  break;
case 'ss_count':
  sql = `SELECT display_name, school, rank_tier, COUNT(*) as value
         FROM dungeon_attempts
         WHERE class_code = ? AND rating = 'SS' AND created_at >= ?
         GROUP BY device_hash
         ORDER BY value DESC
         LIMIT 50`;
  break;
```

- [ ] **Step 2: 在 `LeaderboardScreen.tsx` 新增智子试炼场榜单 Tab**

```typescript
const LEADERBOARD_TYPES = [
  { key: 'wins', label: '试炼胜场' },
  { key: 'ss_count', label: '无伤通关' },
  { key: 'progress', label: '征服进度' },
  { key: 'warrior', label: '班级战神' },
];
```

- [ ] **Step 3: 提交**

```bash
git add cf-workers/api.js src-dungeon/components/screens/LeaderboardScreen.tsx
git commit -m "feat(智子试炼场): 扩展班级排行榜维度"
```

---

### Task 13: 添加副本背景与剧情

**Files:**
- Create: `src-dungeon/assets/dungeon-bg/`（存放背景图）
- Modify: `src-dungeon/data/dungeons.json`
- Modify: `src-dungeon/components/screens/DungeonEntrance.tsx`

**Context:** 每个副本配一张背景图，进入前显示 NPC 开场白。

- [ ] **Step 1: 在 `dungeons.json` 每个副本添加背景图和剧情字段**

```json
{
  "id": "dungeon-01",
  "name": "天机阁",
  "subtitle": "计算机基础",
  "icon": "🏰",
  "description": "...",
  "bgImage": "/dungeon-bg/tianji-ge.png",
  "guardianLine": "...",
  "bossLine": "..."
}
```

- [ ] **Step 2: 在 `DungeonEntrance.tsx` 展示背景图和剧情**

```tsx
<div className="dungeon-entrance" style={{ backgroundImage: `url(${dungeon.bgImage})` }}>
  <div className="npc-dialog">
    <p>{dungeon.guardianName}：{dungeon.guardianLine}</p>
  </div>
  {/* 关卡列表 */}
</div>
```

- [ ] **Step 3: 提交**

```bash
git add src-dungeon/data/dungeons.json src-dungeon/components/screens/DungeonEntrance.tsx src-dungeon/assets/dungeon-bg/
git commit -m "feat(智子试炼场): 副本背景图与剧情引入"
```

---

### Task 14: 添加说明系统

**Files:**
- Create: `src-dungeon/components/screens/TutorialOverlay.tsx`
- Create: `src-dungeon/components/screens/ElementGuide.tsx`
- Modify: `src-dungeon/components/screens/BattleScreen.tsx`
- Modify: `src-dungeon/components/screens/RewardScreen.tsx`

**Context:** 帮助孩子理解战斗机制。

- [ ] **Step 1: 创建 `TutorialOverlay.tsx`**

```typescript
const STEPS = [
  { text: '点击技能，选择你想复习的知识点', target: '.skill-bar' },
  { text: '回答对应的编程题', target: '.question-panel' },
  { text: '答对就能释放技能，答错会变弱哦', target: '.battle-arena' },
];

export function TutorialOverlay({ onClose }: { onClose: () => void }) {
  // 逐步高亮引导
}
```

- [ ] **Step 2: 创建 `ElementGuide.tsx`**

```typescript
export function ElementGuide({ onClose }: { onClose: () => void }) {
  return (
    <div className="element-guide-modal">
      <h3>元素克制手册</h3>
      <p>🔥 火 → 🌪️ 风 → 🟫 地 → 💧 水 → 🔥</p>
      <p>克制：造成 1.5 倍伤害</p>
      <p>被克：只造成 0.7 倍伤害</p>
      <p>⭐ 光：没有克制关系，但很平衡</p>
      <button onClick={onClose}>知道了</button>
    </div>
  );
}
```

- [ ] **Step 3: 在 `RewardScreen.tsx` 展示评级维度**

```tsx
<div className="rating-breakdown">
  <p>正确率：{accuracy}%</p>
  <p>剩余 HP：{remainingHpRatio}%</p>
  <p>使用技能种类：{uniqueSkillCount}/4</p>
  <p>战斗回合：{roundCount}</p>
</div>
```

- [ ] **Step 4: 提交**

```bash
git add src-dungeon/components/screens/TutorialOverlay.tsx src-dungeon/components/screens/ElementGuide.tsx src-dungeon/components/screens/BattleScreen.tsx src-dungeon/components/screens/RewardScreen.tsx
git commit -m "feat(智子试炼场): 添加新手引导、元素手册、评级说明"
```

---

## 验证与收尾

### Task 15: 运行类型检查与构建

- [ ] **Step 1: TypeScript 检查**

```bash
cd /Users/hanliuliu/Desktop/学生成长计划/csp学习助手（师+生）/csp-desktop-pet
npx tsc --noEmit
```

Expected: no errors.

- [ ] **Step 2: 运行单元测试**

```bash
npm test
```

Expected: all tests pass.

- [ ] **Step 3: 构建 Dungeon Web 版本**

```bash
npm run build:dungeon
```

Expected: `dist-dungeon/` generated successfully.

- [ ] **Step 4: 提交**

```bash
git add .
git commit -m "chore(智子试炼场): 类型检查、测试与构建通过"
```

---

## 依赖顺序

```
Task 1 (类型) → Task 2 (数值) → Task 3 (技能) → Task 4 (地牢类型)
  ↓
Task 5 (选题) → Task 6 (升级) → Task 7 (BattleScreen)
  ↓
Task 8 (敌方配置) → Task 9 (奖励) → Task 10 (评级) → Task 11 (周限制)
  ↓
Task 12 (排行榜) → Task 13 (背景剧情) → Task 14 (说明系统) → Task 15 (验证)
```

## 风险点

| 风险 | 缓解 |
|---|---|
| BattleScreen 改动面大，可能破坏原有 quiz 模式 | 保留旧组件备份，出问题可快速回退 |
| 宠物数值不平衡 | 先内部测试 3–5 关，根据胜率调整公式 |
| 题目按标签抽取不足 | 标签映射需要人工校验，不足时 fallback 到全题库 |
| 周挑战次数与现有签到冲突 | 不改动签到系统，地牢次数独立存储 |

---

## Spec Coverage Check

| Spec 要求 | 对应 Task |
|---|---|
| 宠物战斗属性 | Task 1, 2, 6 |
| 4 技能 + 知识点标签 | Task 3, 5, 7 |
| 元素克制 | Task 2, 7, 14 |
| 速度决定先后手 | Task 2, 7 |
| 手动升级回血 | Task 6 |
| 8 副本 40 关保留 | Task 8 |
| 随机金币奖励 | Task 9 |
| S/SS 评级 | Task 10 |
| 每周 5 次挑战 | Task 11 |
| 班级排行榜 | Task 12 |
| 背景 + 剧情 | Task 13 |
| 说明系统 | Task 14 |
| 无交易/无速度奖励/复用签到 | 已确认不实现 |

---

**Plan complete and saved to `docs/superpowers/plans/2026-06-29-智子试炼场-plan.md`.**

**Two execution options:**

1. **Subagent-Driven (recommended)** - I dispatch a fresh subagent per task, review between tasks, fast iteration
2. **Inline Execution** - Execute tasks in this session using executing-plans, batch execution with checkpoints

**Which approach?**
