请生成一张 C++ 教学知识卡图片，主题为「程序阅读与分析 知识卡」，副标题横幅标语「像计算机一样思考，答案自然浮现！」。

【画面要求】竖版约 1024×1536，儿童友好卡通风信息图，浅色背景。顶部居中大标题「程序阅读与分析 知识卡」，左上角放智子IP吉祥物「小麒麟」（奶白色绒毛、粉白渐变双角与鬃毛、琥珀色圆大眼睛、额头金色印记、金色祥云项圈配蓝宝石吊坠、金色火焰状尾巴尖，开心欢迎姿态）和红色 "C++" 标签，标题下是橙色横幅标语（两侧带⭐）。正文由 7 个圆角栏目块组成，每块有序号图标和彩色小标题（红/蓝/绿/紫/橙轮换），白底带浅灰描边和轻投影。底部是红色⚠️「关键提醒」栏，右下角再放一只小麒麟插图（指向知识点的姿态）。图中不要出现其他动物角色，只要小麒麟。

【栏目内容】

一、核心方法：变量跟踪法
读程序不要"想"，要"算"：拿一张纸列出变量表，逐行模拟。
步骤：① 写下所有变量及初值 → ② 每执行一步更新表格 → ③ 循环只算前几轮找规律 → ④ 写出最终输出
示例表头：| 轮次 | i | sum | 说明 |

二、五大常见程序模式（看到就认出来）
1. 累加器：sum += x;   结果通常是求和
2. 计数器：if (条件) cnt++;   统计满足条件的个数
3. 最值：if (x > mx) mx = x;   求最大/最小值
4. 交换逆序：t=a; a=b; b=t; 配合双下标实现逆序
5. 标志位：bool flag = true;  用于判断是否全部满足

三、四大高频陷阱（出题人最爱）
1. 整数除法：5/2=2（不是 2.5），-5/2=-2；想保留小数要 *1.0 或 (double)
2. 运算符优先级：先算比较再算 && 和 ||；& 和 &&、| 和 || 不一样
3. 自增自减：i++ 先用后加，++i 先加后用
4. 循环边界：for(i=0;i<n;i++) 执行 n 次；i<=n 执行 n+1 次

四、溢出与数据范围
- int 范围约 ±21 亿（2×10⁹）：10⁵×10⁵ = 10¹⁰ 会溢出
- 累乘、平方、阶乘最容易爆 int，看到就用 long long
- unsigned 下 0-1 会变成超大数
代码示例：
int a = 100000, b = 100000;
long long c = 1LL * a * b;   // 先转 long long 再乘

五、输出格式陷阱
- 空格在行末还是行首；每行末尾有没有换行
- printf("%03d", x) 补前导零；保留两位小数 printf("%.2f", x)
- 最后一次循环是否多输出一个空格/逗号
- cout 输出 double 默认 6 位有效数字

六、模拟实战示例（完整小例题）
int x = 12, y = 0;
while (x > 0) {
  if (x % 2 == 1) y++;
  x /= 2;
}
cout << y;
跟踪：x=12→6→3→1→0，x%2 为 1 出现 2 次 → 输出 2
（本质：统计二进制中 1 的个数）

七、考场提速技巧
1. 循环只算前 2~3 轮 + 最后 1 轮，中间找规律
2. 函数题先搞清"输入是什么、返回什么、改了哪个全局变量"
3. 递归题画出调用树，标出每层返回值
4. 选项代入法：把选项代回程序验证，比硬算快
5. 遇到看不懂的变量名，给它起个外号（cnt→计数器）

⚠️ 关键提醒：
✅ 读程序靠纸笔模拟，不靠脑子空想
✅ 整数除法直接舍去小数
✅ i++ 与 ++i 结果不同
✅ 累乘平方先想 long long
✅ 循环次数看清 < 与 <=
✅ 选项代入法是最后的兜底招
